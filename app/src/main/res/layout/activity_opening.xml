<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/white_page"
    android:orientation="vertical"
    android:gravity="center"
    tools:context=".ui.opening.OpeningActivity">


    <ImageView
        android:id="@+id/iv_appLogo"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_marginTop="60dp"
        android:src="@drawable/logo"/>

    <!-- Title -->
    <TextView
        android:id="@+id/tv_apptitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="-40dp"
        android:text="SIGNMATE"
        android:fontFamily="@font/font_righteous"
        android:textSize="30sp"
        android:textStyle="bold"
        android:textColor="@color/blue" />

    <TextView
        android:id="@+id/tv_openingdesc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/opening_desc"
        android:fontFamily="@font/font_inter"
        android:textSize="16sp"
        android:textColor="@color/blue"
        android:gravity="center" />

    <LinearLayout
        android:id="@+id/bg_next"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:background="@drawable/ic_backgroundbawah"
        android:layout_marginTop="20dp"
        android:gravity="center">

        <Button
            android:id="@+id/btn_next"
            android:layout_width="match_parent"
            android:layout_height="65dp"
            android:backgroundTint="@color/yellow"
            android:layout_gravity="center_vertical"
            android:layout_marginHorizontal="30dp"
            android:fontFamily="@font/font_inter"
            android:text="NEXT"
            android:textColor="@color/blue"
            android:textSize="16sp" />
    </LinearLayout>
</LinearLayout>
